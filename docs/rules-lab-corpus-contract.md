# Candidate origins and split freeze, revision0.1

Batch5D on verified47f5a87d7c8a672e06c8398b9333889fd44a7e75. Both exact-tip
CI tests pass;697local tests pass114.184s, zero skips. Approved plan hash remains
71517f521ca4da1ab17acc817381e52f3f05e872700ece6ec96c9ae0a59dec6c.
This candidate contract changes no rule, architecture, admission gate or budget.

## Scope and advisory disposition

Add lab_corpus.py, lab_symmetry.py and lab_corpus_candidates.py in research/harness,
with additive tests and a compact frozen candidate index. Existing production,
native, oracle, checker, producer, adapter, origin, supervisor, worker and budget
sources remain byte-identical. No PR20–22, Personal model, live match, historical
fixture/result or browser changes. No paid compute or merge.

The bounded advisory20260907T065400Z succeeded via CLI fallbackclaude-opus-4-8
in182.2s (186.506s wrapper); actual response read. Its useful bounded unit is a
candidate-origin/symmetry/split freeze before certification. Corrections: a
proof-free screen cannot infer all-action equivalence or game values; candidate
counts are not certified counts; no extra undefined margin gate; pending CI is
a wait, not a final stop; the first bootstrap must cover frozen rule order rather
than favor the first/smallest game. No same-cycle re-consult.

## Candidate pool

Freeze16 short authored templates per size, n3 and n4, for all16 definitions in
the existing production-provider order. Maximum512 proposed origins. Templates
may instantiate explicit coordinate selections from immutable board geometry,
but must never search for a better/legal alternative, sample a policy game,
evaluate outcomes, call an opponent or produce a proof. A failed authored action
is an explicit invalid-candidate record, not silently replaced by another action.
Fix all templates/parameters before compiling the full candidate manifest.

Cover openings/contact/connections, pie, two-pass first endings, both acceptance
opportunities and once-resumed endings. Include legal oriented construction and
planted actions for their actual families, zero-point hub occupation where
applicable, and line/fence or group shapes for scoring definitions. Categories
describe intended mechanics only, never asserted winning/optimal choices. Short
known mechanical traces from existing tests may be explicitly transcribed; do not
import snapshots, invent forbidden histories or dynamically import test modules.
Preserve all16 definitions in output, including candidates rejected structurally.

Candidate identity contains stable ID, rule/revision, n, template ID, intended
family, origin configuration and complete action wires. Construction orientations
are part of every semantic action. Configurations use neutral seat labels and
deterministic fixed topology seeds. The authoring table and parameters are hashed.
Real origin compilation uses create_origin then verify_origin; source hashes,
root fingerprint and detached record hash are retained. This is short mechanical
replay, not proof calibration. No actual policy games or proof/search calls in5D.

## Symmetry is a conservative corpus filter, never a state key

Implement the12 rotations/reflections of the integer honeycomb embedding. A60°
rotation maps(x,y) to((x-3y)/2,(x+y)/2); verify exact divisibility. A reflection
may use(x,-y). Vertex/line-point IDs use their actual lattice coordinates. Map
current actual graph edges, stone columns, original scoring points and fixed
initial topology/graph consistently; do not merely rotate stone coordinates
while leaving Y/passage spokes unchanged. Include relevant actor, seat, pass,
resumption, acceptance and extension-phase data, transforming point-valued phase
fields. Distinguish different rules/revisions and board sizes.

Use a deliberately conservative diagram-and-phase equivalence that ignores
forbidden history/journal order and seed labels once their initial geometry is
represented. It may reject strategically distinct histories as duplicates; it
cannot admit the same spatial diagram twice merely because its journal differs.
Document exactly what is included/omitted. Never use this key for superko,
transpositions, optimality, gameplay or source identity. Full origin fingerprints
and analysis keys remain separately preserved. Color interchange is not claimed
as a symmetry because opening/pie identity is asymmetric; spatialD6 is explicit.

Expose pure coordinate and projection/canonicalization helpers for constructed
tests. For production states, project only detached public snapshot/geometry
data, never mutate or manufacture a playable state. Test all16 definitions and
n3–6 with short known mechanical positions, oriented hubs and line graphs. Do not
run any actual-game proof to test symmetry.

## Strict manifest and split rules

The compiler accepts only explicit trusted-local candidate definitions, verifies
fresh origin replay, and computes complete root fingerprints and spatial classes.
Validate finite JSON, exact types, duplicate IDs, known revisions, allowed sizes,
complete action wires and source bytes. Freeze malformed inputs before execution
as errors; do not interpret manifest strings as paths, imports or commands.

Record structural disposition separately: accepted-terminal, single-action,
duplicate, invalid-origin, or candidate. These do not classify WDL/equivalent
optimal alternatives. Keep unknown equivalence explicit. For each ruleset and
size, deduplicate by spatial class, choosing the lexicographically first candidate
ID; sort remaining candidate classes by SHA256 over a fixed split salt plus rule,
size and class key, then alternate development/holdout. A class belongs to only
one split. Include all duplicates/rejections with the representative/reason.

Do not move candidates after observing certification or MCTS outcomes. Return
detached immutable canonical manifests and verify hashes on load/regeneration.
Only counts of structurally eligible candidates are reported. All certified
counts remain zero and admission remains incomplete/unmeasured. Missing eight
positions never becomes a zero score or permission to pad the set.

Oracle10 entries report dual-mechanical-origin availability, not certification.
Legacy6 entries state: independent proof checking is relative to shared
production mechanics; independent_mechanics:false; unqualified for the
independently certified corpus until that gap is resolved under a later approved
scope. Keep their candidates and later bootstrap diagnostics visible. This unit
neither silently waives that requirement nor implements a new legacy engine.

Commit a compact candidate index with definition dispositions/counts, candidate
IDs/splits/origin hashes/spatial keys, source-parent and exact runtime source
bundle hashes, and an external raw manifest hash. Full origin receipts remain
outside the repository. Source-parent is not falsely labeled a commit containing
new uncommitted modules; the later charged job manifest binds the actual committed
source tip and verifies its dependency bytes. No WDL, winning, certified-action or
outcome values occur in the candidate manifest. False/zero qualification counters
are allowed only as explicit status, not evidence labels.

## First charged bootstrap predeclaration, not execution

Before any actual proof/search, freeze a32-task bootstrap: one deterministic
development candidate per n3/n4 for each of16 definitions, round-robin by size
then the frozen rule order. Candidate choice is the first eligible fixed ID,
otherwise an explicit missing task; do not fill it using outcome feedback.
Purpose: measure bounded proof/origin/checker throughput and resource failures,
not certify an entire split or compare games. The task records full fresh-origin
verification, node-bounded production and independent checking; oracle10 additionally
gets its separately produced/checked oracle result, never a production certificate
misidentified as an oracle certificate. Legacy-six claim limits remain explicit.

Predeclare a120s cohort hard reservation with up to8workers and20s per-task cap,
32producer nodes and128checker nodes per mechanics path. All worker/cleanup time
is charged to window1, including unknowns/failures/cancellation; this is not a
measured throughput projection. Budget admission reserves120*1.30=156seconds of
available window/total capacity. The supervisor still enforces120s including its
cleanup reserve. Source/origin preparation inside the real task is charged too.
The node limits define a tiny measurement workload, not a new certification gate.
Actual certification retains the frozen10000-node ceiling. Later cohorts require
measured projections with1.30safety and comparable workload disclosure; no
unjustified linear extrapolation from cheap forced endings to wide game trees.

No bootstrap executes in5D. It requires the concrete hash-frozen task manifest,
runtime source validation, separate trusted proof-job integration, prior synthetic
tests/independent review and exact-tip CI. Keep0officialseconds/windows here.
The three approved MCTS implementations and their synthetic tests remain next;
candidate compilation is not permission to skip those deliverables or claim
admission. Subsequent recipe/holdout runs remain gated by certified corpus minimums.

## Acceptance and continuation

Owners: symmetry/helper tests lab_budget; compiler/validation tests
lab_compatibility; independent invariants/review lab_plan_review; candidate table,
integration/generation, compact artifacts and full verification root. Publish APIs
before cross-module tests. No additional goal-cycle consult or real proof by agents.

TestD6 group closure/exact coordinate maps, oriented adjacency/scoring/phase
projection, rotation/reflection canonical equality, non-equivalent shapes,
history-insensitive conservative dedup versus distinct full fingerprints, source/
manifest tamper, split determinism/disjointness and no outcome labels. All697+
product tests, scoped Ruff/compile/both JS, exact regeneration, independent review
and CI must pass with zero skipped/deleted tests. Preserve negative authoring/tests.
Commit/push specific files, reread guide/hash/check feedback/CI and continue Batch5.
No internal-checkpoint stop, no comparative game before per-ruleset admission.
