# Fresh launch prompt

Use only after the staging preflight and Stop Gate say launch-ready:

> Launch the staged Rules Laboratory run on `codex/rules-lab-v1` in
> `/Users/armand/Development/varde-rules-lab-v1`. Read
> `docs/elves/rules-lab-v1-survival-guide.md` first, then the session JSON,
> learnings, approved plan, and execution log. Execute all six batches in order;
> commit, push, and review each verified batch, then continue autonomously.
> Respect the three resumable 12-hour research windows, eight-worker limit,
> integrity stops, and per-ruleset MCTS prerequisite. Preserve negative results
> and finish honest evidence cards even when dependent matches remain blocked.
> Do not weaken gates, change frozen rules, overwrite Personal models, merge any
> PR, or stop merely at a checkpoint. Stop only under the recorded Stop Gate.
